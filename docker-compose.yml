services:

  mongodb:
    image: mongo:8
    container_name: pulse-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

  redis:
    image: redis:8
    container_name: pulse-redis
    ports:
      - "6379:6379"

volumes:
  mongodb_data:
